---
description: 个人协作规则（需求澄清、优先级、留痕、日报周报、UI与架构、协作优化、数据安全、英文文案与UI连贯、变更联动检索）
alwaysApply: true
---

# 个人协作规则（轻量版）

## 1) 先确认，再执行
- 需求不清晰时，不直接实现，先对目标、范围、验收标准做确认。
- 如存在多种做法，先给出可选方案，再确认执行路径。
- 检查点：开始任何实质执行前。

## 2) 优先级分批推进
- 当需求较多时，先推动明确优先级和先后顺序。
- 采用分批执行：先完成一批高优先级，再处理下一批。
- 检查点：任务拆分、排期、进入下一阶段前。

## 3) 重点变化必须留痕
- 关键变更需要记录：变更原因、影响范围、关键决策依据。
- 无留痕的关键调整，默认不视为完成。
- 检查点：重要改动后、阶段交付前。

## 4) 日报与周报机制
- 每日记录重要决策与重要动作，形成简要日报反馈。
- 每周汇总关键进展、问题与下一步，形成周报反馈。
- 检查点：每日收尾、每周收尾。

## 5) UI 与架构以业务为锚点
- UI 设计与代码架构必须结合真实业务场景，不做脱离业务的优化。
- 方案制定时主动参考 GitHub 和市场上有认可度、讨论度高的优秀案例。
- 检查点：方案设计、评审、实现前复核。

## 6) 持续优化协作
- **反馈闭环**：每完成重要阶段简要复盘顺畅点与卡点，根据痛点迭代规则与模板。检查点：项目里程碑、月底、或明显协作摩擦时。
- **知识沉淀**：优秀方案归档到项目文档，踩坑与否决方案简要记录原因。检查点：方案定稿后、阶段性交付时。
- **节奏校准**：明确响应预期（如 4 小时内确认、复杂问题 24 小时内给方案），设定免打扰时段。检查点：项目启动时约定、响应延迟或打断过多时复盘。

## 7) 计划与反馈文档使用中文
- 给出的 plan、方案说明、阶段性反馈等正式文档一律用中文撰写和呈现。
- 代码注释、变量名等仍可按项目既有习惯（中英文皆可）。

## 8) 确认后再写代码，减少反复修改
- 需求与方案确认清楚后再动笔写代码；不确认就写、写了又删、删了再写视为无效劳动。
- 有歧义或多种实现方式时，先对齐再实现，不靠改代码试错。
- 检查点：落笔写/改代码之前。

## 9) 数据安全与保护
- 敏感信息（API Key、密钥、账号密码等）不得硬编码，一律用环境变量或安全配置管理。
- 设计接口与存储时遵循最小权限与最小数据原则：只收集、只暴露、只持久化必要数据。
- 涉及用户或业务敏感数据时，需考虑访问控制、传输与存储保护（如 HTTPS、脱敏、加密），并在方案中写明保护措施。
- 检查点：新增/修改配置或数据流时、上线前复核。

## 10) 英文文案规范
- 所有英文文案书写采用首字母大写（Capitalizing Initials / Title Case），全项目统一。
- 检查点：新增或修改按钮、标签、提示、标题等可见英文文案时。

## 11) UI 设计优先同页连贯
- 做 UI 设计或改版时，优先使用当前页面内已有的组件、样式、交互方式，保证视觉与交互连贯性。
- 新增元素需与同页既有风格一致，避免同一页面内多种不一致的写法。
- 检查点：新增或修改界面元素时。

## 12) 页面/模块变更时的联动检索
- 当某页面或模块发生改名、拆并、职责调整等变更时，必须自动检索全项目：所有引用该处或提到该处的文案、注释、路由、组件名、文档等。
- 对检索结果逐一判断并优化，使上下文与跨页面的文案、代码保持连贯，避免遗留旧名称、死链、过时说明。
- 检查点：完成页面/模块结构性修改后、提交前。
